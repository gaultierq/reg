<%= render 'shared/notice' %>

<div class="uk-flex-middle uk-margin-medium" uk-grid>
  <div class="uk-width-expand@m">
    <h1 class="uk-heading-primary">Tap Templates</h1>
  </div>
  <div class="uk-width-auto@m">
    <%= link_to 'New Tap Template', new_tap_template_path, class: 'uk-button uk-button-primary' %>
  </div>
</div>

<%= render 'shared/table', locals: { id: 'tap_templates_table', dom: 'lrtip', paging: false, info: false,
                                     columns: %w[id name article_number], url: tap_templates_path(format: :json) } do %>
  <thead>
  <tr>
    <th>id</th>
    <th>name</th>
    <th>article number</th>
  </tr>
  </thead>
<% end %>
