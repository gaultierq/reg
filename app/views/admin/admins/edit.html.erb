<div class="uk-section uk-section-default">
  <div class="uk-container">
    <div class="uk-flex-middle uk-margin-medium" uk-grid>
      <div class="uk-width-expand@m">
        <h1 class="uk-heading-primary">Editing Admin</h1>
      </div>
      <div class="uk-width-auto@m">
        <%= link_to 'Show', admin_admin_path(@admin), class: 'uk-button uk-button-primary' %>
        <%= link_to 'Destroy', admin_admin_path(@admin), method: :delete, data: { confirm: 'Are you sure?' }, class: 'uk-button uk-button-danger' %>
      </div>
    </div>

    <%= render 'form', admin: @admin %>
  </div>
</div>
