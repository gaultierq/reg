<div class="uk-section uk-section-default">
  <div class="uk-container">
    <div class="uk-flex-middle uk-margin-medium" uk-grid>
      <div class="uk-width-expand@m">
        <h1 class="uk-heading-primary">Editing Industrial Unit</h1>
      </div>
      <div class="uk-width-auto@m">
        <%= link_to 'Show', admin_industrial_unit_path(@industrial_unit), class: 'uk-button uk-button-primary' %>
        <%= link_to 'Destroy', admin_industrial_unit_path(@industrial_unit), method: :delete, data: { confirm: 'Are you sure?' }, class: 'uk-button uk-button-danger' %>
      </div>
    </div>

    <%= render 'form', industrial_unit: @industrial_unit %>
  </div>
</div>
