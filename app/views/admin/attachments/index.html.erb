<div class="uk-section uk-section-default">
  <div class="uk-container">
    <%= render 'shared/notice' %>

    <div class="uk-flex-middle uk-margin-medium" uk-grid>
      <div class="uk-width-expand@m">
        <h1 class="uk-heading-primary">GED</h1>
      </div>
      <div class="uk-width-auto@m">
        <%= link_to 'Nouveau Document', new_admin_attachment_path, class: 'uk-button uk-button-primary' %>
      </div>
    </div>

    <div class="uk-section-small uk-padding-remove-top">
      <h3 class="uk-heading-divider">Instruction de service</h3>
      <%= render 'shared/table', locals: { id: 'attachment_instruction_service_table', dom: 'lrtip', paging: false, info: false } do %>
        <thead>
        <tr>
          <th>id</th>
          <th>nom</th>
          <th data-orderable="false">actions</th>
        </tr>
        </thead>

        <tbody>
        <% @attachments.where(kind: :instruction_service).each do |attachment| %>
          <tr>
            <td><%= attachment.id %></td>
            <% if attachment.pdf.attached? %>
              <td><%= attachment.pdf.blob.filename %></td>
            <% else %>
              <td></td>
            <% end %>
            <td class="uk-flex uk-flex-center">
              <ul class="uk-iconnav">
                <li>
                  <%= link_to admin_attachment_path(attachment) do %>
                    <span uk-icon="search", uk-tooltip="Détails"></span>
                  <% end %>
                </li>
                <li>
                  <%= link_to edit_admin_attachment_path(attachment) do %>
                    <span uk-icon="pencil", uk-tooltip="Modifier"></span>
                  <% end %>
                </li>
                <li>
                  <%= link_to admin_attachment_path(attachment), method: :delete, data: { confirm: 'Êtes-vous sûr ?' } do %>
                    <span uk-icon="trash", uk-tooltip="Supprimer"></span>
                  <% end %>
                </li>
              </ul>
            </td>
          </tr>
        <% end %>
        </tbody>
      <% end %>
    </div>

    <h3 class="uk-heading-divider">Instrumentation, détecteur de position</h3>
    <%= render 'shared/table', locals: {id: 'attachment_instrumentation_position_table', dom: 'lrtip', paging: false, info: false} do %>
      <thead>
      <tr>
        <th>id</th>
        <th>nom</th>
        <th data-orderable="false">actions</th>
      </tr>
      </thead>

      <tbody>
      <% @attachments.where(kind: :instrumentation_position).each do |attachment| %>
        <tr>
          <td><%= attachment.id %></td>
          <% if attachment.pdf.attached? %>
            <td><%= attachment.pdf.blob.filename %></td>
          <% else %>
            <td></td>
          <% end %>
          <td class="uk-flex uk-flex-center">
            <ul class="uk-iconnav">
              <li>
                <%= link_to admin_attachment_path(attachment) do %>
                  <span uk-icon="search" , uk-tooltip="Détails"></span>
                <% end %>
              </li>
              <li>
                <%= link_to edit_admin_attachment_path(attachment) do %>
                  <span uk-icon="pencil" , uk-tooltip="Modifier"></span>
                <% end %>
              </li>
              <li>
                <%= link_to admin_attachment_path(attachment), method: :delete, data: {confirm: 'Êtes-vous sûr ?'} do %>
                  <span uk-icon="trash" , uk-tooltip="Supprimer"></span>
                <% end %>
              </li>
            </ul>
          </td>
        </tr>
      <% end %>
      </tbody>
    <% end %>

    <h3 class="uk-heading-divider">Instrumentation, pilotage</h3>
    <%= render 'shared/table', locals: {id: 'attachment_instrumentation_pilotage_table', dom: 'lrtip', paging: false, info: false} do %>
      <thead>
      <tr>
        <th>id</th>
        <th>nom</th>
        <th data-orderable="false">actions</th>
      </tr>
      </thead>

      <tbody>
      <% @attachments.where(kind: :instrumentation_pilotage).each do |attachment| %>
        <tr>
          <td><%= attachment.id %></td>
          <% if attachment.pdf.attached? %>
            <td><%= attachment.pdf.blob.filename %></td>
          <% else %>
            <td></td>
          <% end %>
          <td class="uk-flex uk-flex-center">
            <ul class="uk-iconnav">
              <li>
                <%= link_to admin_attachment_path(attachment) do %>
                  <span uk-icon="search" , uk-tooltip="Détails"></span>
                <% end %>
              </li>
              <li>
                <%= link_to edit_admin_attachment_path(attachment) do %>
                  <span uk-icon="pencil" , uk-tooltip="Modifier"></span>
                <% end %>
              </li>
              <li>
                <%= link_to admin_attachment_path(attachment), method: :delete, data: {confirm: 'Êtes-vous sûr ?'} do %>
                  <span uk-icon="trash" , uk-tooltip="Supprimer"></span>
                <% end %>
              </li>
            </ul>
          </td>
        </tr>
      <% end %>
      </tbody>
    <% end %>

    <h3 class="uk-heading-divider">Instrumentation, autres instrumentation</h3>
    <%= render 'shared/table', locals: {id: 'attachment_instrumentation_autre_table', dom: 'lrtip', paging: false, info: false} do %>
      <thead>
      <tr>
        <th>id</th>
        <th>nom</th>
        <th data-orderable="false">actions</th>
      </tr>
      </thead>

      <tbody>
      <% @attachments.where(kind: :instrumentation_autre).each do |attachment| %>
        <tr>
          <td><%= attachment.id %></td>
          <% if attachment.pdf.attached? %>
            <td><%= attachment.pdf.blob.filename %></td>
          <% else %>
            <td></td>
          <% end %>
          <td class="uk-flex uk-flex-center">
            <ul class="uk-iconnav">
              <li>
                <%= link_to admin_attachment_path(attachment) do %>
                  <span uk-icon="search" , uk-tooltip="Détails"></span>
                <% end %>
              </li>
              <li>
                <%= link_to edit_admin_attachment_path(attachment) do %>
                  <span uk-icon="pencil" , uk-tooltip="Modifier"></span>
                <% end %>
              </li>
              <li>
                <%= link_to admin_attachment_path(attachment), method: :delete, data: {confirm: 'Êtes-vous sûr ?'} do %>
                  <span uk-icon="trash" , uk-tooltip="Supprimer"></span>
                <% end %>
              </li>
            </ul>
          </td>
        </tr>
      <% end %>
      </tbody>
    <% end %>

    <h3 class="uk-heading-divider">Actionnement, actionneur</h3>
    <%= render 'shared/table', locals: {id: 'attachment_actionnement_actionneur_table', dom: 'lrtip', paging: false, info: false} do %>
      <thead>
      <tr>
        <th>id</th>
        <th>nom</th>
        <th data-orderable="false">actions</th>
      </tr>
      </thead>

      <tbody>
      <% @attachments.where(kind: :actionnement_actionneur).each do |attachment| %>
        <tr>
          <td><%= attachment.id %></td>
          <% if attachment.pdf.attached? %>
            <td><%= attachment.pdf.blob.filename %></td>
          <% else %>
            <td></td>
          <% end %>
          <td class="uk-flex uk-flex-center">
            <ul class="uk-iconnav">
              <li>
                <%= link_to admin_attachment_path(attachment) do %>
                  <span uk-icon="search" , uk-tooltip="Détails"></span>
                <% end %>
              </li>
              <li>
                <%= link_to edit_admin_attachment_path(attachment) do %>
                  <span uk-icon="pencil" , uk-tooltip="Modifier"></span>
                <% end %>
              </li>
              <li>
                <%= link_to admin_attachment_path(attachment), method: :delete, data: {confirm: 'Êtes-vous sûr ?'} do %>
                  <span uk-icon="trash" , uk-tooltip="Supprimer"></span>
                <% end %>
              </li>
            </ul>
          </td>
        </tr>
      <% end %>
      </tbody>
    <% end %>
  </div>
</div>