<div class="uk-section uk-section-default">
  <div class="uk-container">
    <%= render 'shared/notice' %>

    <div class="uk-flex-middle uk-margin-medium" uk-grid>
      <div class="uk-width-expand@m">
        <h1 class="uk-heading-primary">Robinets</h1>
      </div>
      <div class="uk-width-auto@m">
        <%= link_to 'Nouveau Robinet', new_admin_tap_path, class: 'uk-button uk-button-primary' %>
      </div>
    </div>

    <%= render 'shared/table', locals: { id: 'taps_table', dom: 'lrtip', paging: false, info: false } do %>
      <thead>
      <tr>
        <th>id</th>
        <th>Nom</th>
        <th>N° puce RFID</th>
        <th>N° série REG</th>
        <th>N° TAG client</th>
        <th>Date de fabrication</th>
        <th>N° vente REG</th>
        <th>N° commande client</th>
        <th>N° article</th>
        <th>Unité Industrielle</th>
        <th data-orderable="false">Actions</th>
      </tr>
      </thead>

      <tbody>
      <% @taps.each do |tap| %>
        <tr>
          <td><%= tap.id %></td>
          <td><%= tap.name %></td>
          <td><%= tap.rfid_number %></td>
          <td><%= tap.serial_number %></td>
          <td><%= tap.number_customer_tag %></td>
          <td><%= tap.manufacturing_date %></td>
          <td><%= tap.sales_number %></td>
          <td><%= tap.customer_order_number %></td>
          <td><%= tap.article_number %></td>
          <td><%= tap.industrial_unit&.name %></td>
          <td class="uk-flex uk-flex-center">
            <ul class="uk-iconnav">
              <li>
                <%= link_to admin_tap_path(tap) do %>
                  <span uk-icon="search", uk-tooltip="Détails"></span>
                <% end %>
              </li>
              <li>
                <%= link_to edit_admin_tap_path(tap) do %>
                  <span uk-icon="pencil", uk-tooltip="Modifier"></span>
                <% end %>
              </li>
              <li>
                <%= link_to admin_tap_path(tap), method: :delete, data: { confirm: 'Êtes-vous sûr ?' } do %>
                  <span uk-icon="trash", uk-tooltip="Supprimer"></span>
                <% end %>
              </li>
            </ul>
          </td>
        </tr>
      <% end %>
      </tbody>
    <% end %>
  </div>
</div>
